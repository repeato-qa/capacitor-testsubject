// ui/src/utils/types.ts

export interface Product {
  id: string;
  name: string;
  description: string;
  imageUrl: string;
  // Add other properties like price if needed in the future
}

// Add any other shared types for the UI here
